"use strict";


// Eingabedaten holen
let titel_1 = prompt("Titel: ");
console.log(titel_1)

let typ = prompt("Typ (Einnahme / Ausgabe): ");
console.log(typ)

let betrag = prompt("Betrag (in Cent): ");
console.log(betrag)

let datum = prompt("Datum (jjjj-mm-tt): ");
console.log(datum)

//  Gesamtbilanz
let einnahmen;
let ausgaben;
let bilanz;
